// Edit photo

const editPhoto = document.querySelector(".photo-edition");






















/**const editPhoto = document.querySelector(".photo-edition");
var uploaded_image = "";

editPhoto.addEventListener("change", function(){
    const reader = new FileReader();
    reader.addEventListener("load", () => {
        uploaded_image = reader.result;
        document.querySelector(".personal-photo").style.backgroundImage = 'url(${uploaded_image})'
    });
    reader.readAsDataURL(this.files[0]);
})


const editText = document.getElementsByClassName(".about");
editText.innerHTML = "";

*/